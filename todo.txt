X tab switching
X search page design
    X list of images
    X search by name input
    X tag fillter:
        X show a tag search popup
        X click tag to select it for the fillter
X palet page design
    X list of images
    X selected image half screen view
    X remove buttons
    X open folder button
X make search page ui elements
X add search page logic
X make pallet page ui elements
X add pallet page logic
X save pallet images in a pallet folder
X make the 3 image columns more equil with more dynamic logic
X add image selected popup to search
X add image tag editing popup with logic

* fix scrolling
* more tags at a time
* scroll bar
* fix tag sizing

Notes:
* kv file added widgets will not get the class functions. only if they are added with code (Widget()).

Future:
* add images to search blacklist
* saved pallets
* load next 100 images
* or/and/not search modes for each tag
* editable image list layout - colum count
* skip image in TaggingPage
* bug: deleting a tag in search page refreshes the tag list but the tag search still has text in it that gets ignored

